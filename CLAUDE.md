# MixDictate — 给以后的自己看

## 最重要的一条

**这个项目里几乎所有故障都是无声的。**

macOS 不会因为权限没给就报错，`AVAudioConverter` 不会因为声道数对不上就报错，
`AVAudioEngine` 不会因为麦克风被拒就停下 —— 它们照常运行，只是送来一串零。
而"一串零"和"用户没说话"在代码里逐字节相同。

所以：**每一条可能静默失败的路径，都必须显式检查并说出来。** 已经因为违反
这条走了好几次弯路，每次都是先怀疑了错的东西。

---

## 犯过的错（按被骗的程度排）

### 1. 回声消除把麦克风变哑 —— 伪装成权限问题

`setVoiceProcessingEnabled(true)` 会把输入格式从单声道变成多声道（常见 5 声道）。
`AVAudioConverter` 在声道数不匹配又没有 `channelMap` 时**不报错，直接输出全零**。

现象：权限正常、引擎在跑、tap 正常回调、每个采样都是 0。
排查方向被带到了 TCC 上，浪费了好几轮。

- 修复：`converter.channelMap = [0]`
- 兜底：录到全静音而回声消除开着 → 自动关掉回声消除并告诉用户
- **不变量：回声消除是锦上添花，能听见是底线。两者绝不能互换位置。**

### 2. 回声消除压低整台电脑的音量

语音处理单元是给 VoIP 用的，一初始化 macOS 就把其他所有音频压低。
控制它的 `kAUVoiceIOProperty_DuckNonVoiceAudio` **只有 iOS 有且已废弃**。

- 结论：macOS 上关不掉，所以 `echoCancellation` **默认 false**
- 关闭必须是**主动动作**（`setVoiceProcessingEnabled(false)`），不能只是
  "下次不再打开" —— 单元只要还活着就一直压低

### 3. 重编译让 TCC 授权失效 —— 开关看着还是开的

ad-hoc 签名没有身份，macOS 拿二进制哈希当身份。重编译一次哈希就变，
麦克风和辅助功能授权全部对不上，**而系统设置里的开关照常显示为开**。

- 现象 A：麦克风只送来静音
- 现象 B：按快捷键完全没反应（全局按键监听本身就需要辅助功能权限）
- 修复：`scripts/signing.sh setup` 建固定签名身份
- **不变量：不要对用户说"检查一下权限"——他会去看，发现开关是开的，然后卡住。**
  要说清楚是授权失效了，并给出可执行的下一步。

### 4. 自动更新的活锁 —— "我改了但没生效"

`autoupdate.sh` 有条规则"三分钟内用过听写就跳过这轮"。单独看是对的，
但用户一边测一边等新版本时，每轮都在三分钟内，**更新永远排不上队**。

- 修复：推迟有上限（10 分钟），外加 `autoupdate.sh now`
- **不变量：任何"稍后再做"的保护都要有超时，否则它会变成"永远不做"。**

### 5. NSHostingController 锁死窗口尺寸

它默认按 SwiftUI 内容的理想尺寸给窗口装约束。于是窗口被撑大且**小不下去**，
加了 `ScrollView` 也没用 —— 窗口不能变小，滚动区就永远有富余空间。

- 修复：`hosting.sizingOptions = []`，并在赋值 contentViewController **之后**
  再 `setContentSize`

### 6. MLX 的 GPU stream 是线程局部的

在 A 线程加载模型、B 线程推理 → `There is no Stream(gpu, 1) in current thread.`
`asyncio.to_thread` 正好踩中。

- 修复：`ThreadPoolExecutor(max_workers=1)`，加载和推理都在它那唯一的线程上
- 有回归测试 `test_thread_affinity.py` 钉住

### 7. Metal 计算核按张量形状编译

只用一个 0.6 秒的样本预热，只编出那个形状的核。用户真正说的话形状不同，
还要现编 —— 表现成"用得越多越快"。

- 修复：冷启动用 0.8/2.5/6 秒各热一遍
- 有测试 `test_warmup.py` 钉住（改回单一长度看起来只是"省两次推理"）

### 8. 停顿处被自动加句号

模型判断句子结束主要看停顿长短，而"在想下一句"和"这句说完了"在音频里
是同一件事。**试过按停顿时长猜，猜错的比猜对的多。**

- 声音侧：把长静音压短（`maxPauseSeconds`），削掉线索本身
- 文字侧：句号后面跟着「然后/但是/所以」等接续词 → 降级成逗号
- **不变量：只降级标点，绝不删字。** 多余的标点用户一眼能看见并改掉，
  删掉的内容他根本不知道曾经存在过。

### 8b. 长句一个逗号都没有 —— 同一件事的反面

压短停顿削掉的不只是"句末"的线索，"分句"的线索也一起削了。说得顺的时候
分句处本来就不停，于是一句话能拖很长。

- 文字侧补：接续词前面加逗号，且要求前面攒够 8 个字
- **同样只加标点，不动字**

### 8c. 人声门限吃字 —— 我自己加的功能反过来伤了底线

门限第一版有三个错，叠加起来表现成"句子中间少了几个字"：

1. **开关同一个阈值，没有回滞。** 语音响度一直在起伏，一个字的尾音掉到
   门限以下再回来，中间那截就被写成静音了。
   → **开门和关门不是同一个量级的判断**：开门要严（外放够不着门限，
   所以开不了门），关门要松（门被你的声音打开后就该一直开到你真说完）。
   关门线取 `max(noiseFloor, voiceThreshold * 0.15)`，配 0.8 秒保持，
   实际上等于"要安静下来才关"。
   拿开门那把尺子量关门，句末降调、字的尾音、轻声字全会被切掉。
2. **默认值 0.05 太高。** 正常说话峰值 0.2–0.6，但轻声字、词尾、词的第二个
   音节常常只有 0.02–0.06 —— 门限正好切在字中间。→ 默认改 0.02。
3. **判断"这段要不要转写"用的是"有没有越过门限"。** 于是"你说得轻"和
   "这段没人说话"变成同一件事，那一整段连转写请求都不发。
   → 分开记 `lastLoudByte`（越过门限）和 `lastSoundByte`（听到声音），
   跳过转写只看后者。

**最后的结论：`voiceThreshold` 默认 0（关闭）。**

上面那些补救（回滞、开口前多压一秒、尾部一律放行）都只在门限**最终会打开**
时才有用。而门限有个绕不过去的漏洞：它只能按响度判断，而"整句都说得轻"和
"这是外放漏进来的"在响度上就是同一件事 —— 一路轻声说的那句话，门限可能
从头到尾没开过，整句都没了。补救救不了这种情况。

它是给"必须外放着听写"准备的开关，不是常备件。真要隔绝外放只有戴耳机。

**这条是最该记住的：为了挡外放加的东西，把"能出字"这条底线弄坏了。**
跟回声消除那次是同一个错误 —— 增强功能永远不能压过底线。
挡不住外放只是没帮上忙，吃掉字是把事情弄坏了，两者代价根本不对等。

一句话的**两端**还要单独保：

- **开口之前**把音频一直压在手里（最多约一秒），门限一开就整批放行。
  不这样的话第一个字必丢：门限起始关着，而起音是渐强的 —— 等它爬过门限，
  前面那点已经被判成静音写出去了。
- **录音尾部**（前瞻里压着的那几块）一律放行，不做判断。那正是最后一个字的
  尾音，而句末降调时它响度本来就低。多留一点环境音没人注意，少一个字一眼可见。

日志和 `verify.sh` 现在都会报"门限挡掉了多少秒"，因为这个失败原来完全不可见。

### 8d. 实时显示总是差最后一个字

三处叠加，全在"为了省一点开销"上：

1. **前瞻压着的音频不参与预览。** 门限的前瞻把最新的几块压在手里没写进
   `pcm`，而预览读的是 `pcm` —— 于是预览永远落后一个前瞻的长度。
   把前瞻从 1 块加到 3 块之后这个滞后被放大了三倍（我自己弄坏的）。
   → 纯预览（没有边界）时把压着的那几块也带上。有边界时**不能**带，
   否则那截会在下一段被再转一次，变成重复出字。
2. **中间结果最少 0.5 秒才发** → 最新的半秒永远不显示，正好是刚说完的字。
   改成 0.25 秒。预览的噪音下一轮就被改掉了，代价不对等。
3. **"尾部没怎么长就复用上一次中间结果"的捷径** → 最后 0.4 秒永远不会被
   转写，而 0.4 秒足够装下一个字。删掉了。省一次推理换掉一个字，不成立。

### 8e. 实时写入吞字 —— 丢的不是识别结果，是按键事件

`sendBackspaces` 和 `typeUnicode` 在紧循环里连发 CGEvent，中间没有任何间隔。
目标 App 的事件队列被这样灌会**丢事件**。

致命的是后果会累积：`LiveInserter` 对"已经写进去多少字"的记忆，是照着
**发出去的事件**算的。丢一个事件这份记忆就永久偏了，之后每次改写都在错误的
基础上退删 —— 越改越歪。而"整理"那一次改动最大（完整重转的结果跟拼接出来的
实时文本差最多），积累的偏差正好在那里爆出来。

所以症状是"实时的时候还行，整理之后吞字"，而浮层模式完全没问题 ——
**它根本不发按键事件。**

- 事件之间 1.2 毫秒，退格批和打字批之间 12 毫秒
- 退格 + 补字必须是**一次**调用，分开发会让两次改写交错
- 全部排在一条**串行后台队列**上：串行是为了不交错，后台是因为要 sleep，
  在主线程 sleep 会把浮层和定时器一起卡住

### 8f. 正在转写时按说话键 —— 整段被丢掉，只响一声

`beginRecording` 原来只允许 `.idle`/`.failed`，转写中按键直接 `return`。
而转写要一秒多，**把一段话分几次说的时候，下一次按下经常正落在这个窗口里**，
那一整段就没了 —— 用户只听到"哔"，不知道自己刚说的全丢了。

录音器那时其实是空闲的（`endRecording` 里已经 stop 过），挡住它的只有 state。

- 允许从 `.transcribing` 开始新一轮
- 但这样一来会有两段并行：**新一轮开始不能让上一段作废**。
  原来的 `session == sessionID` 判据是为 Esc 取消写的，用在这里会把上一段
  的结果丢掉 —— 改成只看"有没有被取消"（`cancelledSessions`）。
- 上一段的结果回来时如果用户已在说下一段：文字照样插入（本来就要接起来），
  但**不能动状态和浮层**，也**不能走实时写入的差异改写** —— 那个改写器
  记的是新一段的内容，拿旧文字去比对会退格删到新一段的字上。

### 8g. 长录音整段消失 —— await 之后才去读实例变量

长录音（超过 `fullPassMaxSeconds`）走拼接：`committedText` + 最后一段重转。
那个拼接写在 `await` **之后**：

```swift
let tail = try await client.transcribe(wav: audio.tail)
let text = committedText + tail        // ← 读的是"现在"的值
```

而 `beginRecording` 会把 `committedText` 清空。用户在这一秒多里按下说话键开始
说下一段，回来读到的就是**新一段的空字符串** —— 拼出来只剩最后一小段，
前面全没了。

- 修复：`await` 之前先拷成局部变量
- **通用教训：跨 `await` 读实例变量，读到的是"现在"的值，不是"当时"的值。**
  这个竞态在"转写中不许按键"时撞不上，允许接着说之后每次都撞 ——
  一个改动让另一处沉睡的 bug 变成必现。
- 顺带把 `fullPassMaxSeconds` 从 45 提到 120：完整重转不光更准，还**不可能
  丢段落**，拼接那条路要靠一串中间结果都正确累积，环节多得多。

### 8h. 实时写入的根因：一份从不核对的记忆

前面 8e 修的是"事件被丢掉"，那只是诱因之一。根因是结构性的：

**`LiveInserter` 是整个 App 里唯一持有"输入框现在是什么样"这份记忆、
并且照着它做破坏性操作（退格）的地方，而这份记忆从来没跟现实核对过。**
它按"我们发出去的事件"推算。

能让它失准的事情全都不受我们控制：事件被丢、用户自己动光标改字、输入法
组字、App 自动补全/缩进/纠错、焦点跑到别处。**偏一个字符，之后每次改写都
在错误的基础上退删，而且累积。**

非实时模式没有这个问题 —— 只在最后插入一次，不持状态、不退格。
用户的原话是那边"好用很多"，这就是原因。

修法不是继续补漏，是**退格之前先核对**：

- 把光标前面那几个字读回来（`kAXStringForRange`），跟记忆比对
- 对不上 → **什么都不做**（不删也不打），标记成不可信，停止实时改写
- 读不回来（控件不支持）→ 只放行 ≤20 字的小改动。
  **没法确认不等于确认通过**
- 停下之后，完整结果走剪贴板兜底，并明确告诉用户

### 8h-2. 核对本身把界面卡死了 —— AX 调用是同步跨进程

上面那个"退格前先核对"用的是辅助功能接口，而 `AXUIElementCopy*` 是**同步的
跨进程 IPC**：目标 App 一忙或者不响应，调用方就一直等，默认超时好几秒。
而我是在**主线程上、每次要退格之前**调它 —— 整个 App 冻住，浮层卡在"整理中"。

- 修复：`AXUIElementSetMessagingTimeout` 设 0.15 秒
- **不回话就当"读不到"处理**，走没法核对那条路。
  宁可核对不了，也不能把界面卡死。
- 通用教训：**任何跨进程同步调用都要有超时**，尤其是在主线程上。

### 8i. 说到一半点走了 —— 文字落进别人的输入框

听写要好几秒，这期间用户可能点到别处。不比对焦点的话，文字会插进**另一个
输入框**，用户以为自己说的那一大段直接消失了。

- 按下说话键时记下焦点（前台 App 的 pid + 聚焦控件），插入前比对
- 变了就**绝不插入** —— 插进别人的输入框比不插糟得多
- 但**绝不丢**：复制到剪贴板，浮层把全文显示出来并说明原因

### 9. 叠词合并会删掉用户想说的话

"超级超级好"是刻意强调，不是卡壳。

- `collapseRepeats` **默认 false**
- 同上那条不变量：删错的代价远大于留着噪音

### 10. macOS bash 3.2 的多字节变量展开

`"$VAR中文"` 会被当成变量名的一部分 → `unbound variable`。Linux 上的新版
bash 不会这样，shellcheck 也查不出来。

- CI 里有 grep 守卫。**它已经抓到过我自己写的一次。**

---

### 12. 改说话键时按普通键没反应 —— 又是无声失败

捕获时只监听了 `flagsChanged`（修饰键事件）。用户按回车产生的是 `keyDown`，
那个监听器**根本收不到** —— 既不改也不报错，界面还显示着原来的键，
看上去就是"改不了"。

- 同时监听 `.flagsChanged` 和 `.keyDown`
- 不能用的键要**说出来**并说清为什么（普通键按住会一直往输入框打字，
  而全局监听拦不住它 —— 只有本地监听能吞事件）
- 同一条老规矩：**不能默默无视用户的操作。**

### 13. 浮层拖动触发了系统分屏

`isMovableByWindowBackground` 走的是系统的窗口拖动，会触发 macOS 的贴边
分屏提示。而这个浮层根本不是正经窗口，被塞进分屏毫无意义。

- `panel.isMovable = false`，在 `NSVisualEffectView` 子类里按鼠标位移
  自己 `setFrameOrigin`

### 14. 界面上的开关反映的是「系统状态」时，不能只记在自己心里

开机自启装在 launchd 里，不在 `config.json` 里。它可能被命令行
（`scripts/autostart.sh`）改掉，也可能被用户在系统设置里删掉。所以：

- 开关的值每次都**问一遍 launchd**，不缓存、不写进配置文件
- 改完**立刻生效**，不跟着「保存」走 —— 保存成功了但自启没装上就看不出来
- 装完**复查一遍**（`launchctl bootstrap` 返回 0 不代表任务真的在）
- 失败就把开关**拨回去**并说出原因。开关停在「开」而系统里其实没装，
  正是这个项目里最典型的那种谎
- Swift 那边和脚本那边必须用**同一个 label**，`verify.sh` 会核对 ——
  两边各写一个的话，界面和命令行会各说各话

没用 `SMAppService`：它要正经开发者签名，而这个 App 是 ad-hoc 签的，
失败时给的错误还很含糊。宁可自己写 plist，也要能把失败原因说出来。

### 15. 关掉开机自启把 App 当场关掉了

`launchctl bootout` 不只是卸载任务，它会**把那个任务正在跑的进程 SIGTERM 掉**。
而"关掉开机自启"这个开关是在 App 自己的设置窗口里 —— 用户点一下取消勾选，
整个 App 当场退出，设置窗口跟着消失。

同一个 `bootout` 在 `enable()` 里也有（bootout → bootstrap 的老套路），
所以**打开**开关时也会先把自己杀掉。

- 先删 plist —— 决定"下次登录还起不起"的本来就只有这个文件
- 只在 launchd 管的**不是自己**时才 `bootout`（比对 `launchctl print` 里的 pid）
- 开关的值改成看 plist 在不在。`launchctl print` 查的是"这次会话里加载没加载"，
  跟"下次登录起不起"是两件事，拿它当开关的值会答非所问
- **通用教训：把一个系统命令包进 UI 开关之前，先弄清楚它除了你要的那件事
  还顺手干了什么。** `bootout` 的文档写的是"卸载"，杀进程是附带的。

顺带修掉的两个：

- **`KeepAlive` 让「退出」失效。** 装了开机自启之后，从菜单里退出 App，
  launchd 10 秒后把它拉回来 —— 表现成"关不掉"。开机自启的意思是登录时起一次，
  不是"你不许退出"。去掉了。
- **`bootstrap` 会拉起第二个实例。** 勾上开关时 job 带着 `RunAtLoad` 加载，
  而 App 已经在跑了 —— 两个菜单栏图标长得一模一样，但有两套热键监听和两个
  转写服务抢同一个端口。启动时加了单实例检查，后来的那个直接退出。

### 16. 更新之后跑的还是旧代码 —— 三个改动各自都对，凑在一起坏了

症状是一串看起来毫不相干的事：菜单栏图标没了、不再弹麦克风授权、
按 Option 还有浮层而且字也基本对，**但文字插不进输入框**。

链条是这样的：

1. `install.sh` 先 `pkill` 旧 App —— 而老版本的开机自启 plist 里有
   `KeepAlive`，launchd **当场又拉起一份旧版**。
2. 接着 `rm -rf /Applications/MixDictate.app` 再 `cp` 新的。那个刚被拉起来
   的进程还活着（inode 还开着），但它的 bundle 已经被换掉了。
3. 最后 `open` 新版 —— 我刚加的单实例检查发现"已经有一个在跑"，
   **新版自己退出了**。

于是留在场上的是一个跑着旧代码、bundle 已经不存在的进程。热键还灵，
因为监听是它当初装上的；但辅助功能的授权按签名记，签名对不上了，
**CGEvent 和 AX 调用就静默地什么都不做** —— 又是一次"没报错但没生效"。

三处一起改：

- **单实例检查不能只看"有没有别人在跑"，要看那个人还能不能用。**
  判据：**启动时间比它要跑的那个二进制还早的实例，跑的一定是旧代码。**
  这种直接 `terminate()` 掉再接管；只有跟自己同代码的实例才值得让路。
- `install.sh` 动 bundle 之前先 `launchctl bootout`，`pkill` 之后**等它真死**
  （轮询 10 秒，再 `-9`），装完再把开机自启补回去。
- 装完**查一遍它到底起来没有**。菜单栏 App 没起来和起来了长得一模一样。

`verify.sh` 现在会比对进程启动时间和二进制的 mtime，专门抓这种僵尸。

**通用教训：单实例检查是个"谁让谁"的决定，而这个决定必须基于对方是否
还健康，不能只基于对方存在。** 让路给一个坏掉的实例，比同时跑两个更糟 ——
两个至少都是能用的。

### 17. 菜单栏图标不见了 —— App 好好地跑着，只是没有入口

热键有反应、字能插进输入框、剪贴板里也有 —— 唯独菜单栏上没有图标。

按住 Command 把状态栏图标拖出菜单栏，macOS 会把「隐藏」**记进偏好设置**，
之后每次启动都不再显示。`NSStatusItem` 照样创建成功，`isVisible` 默认值
被那条记录覆盖，代码里看不出任何异常。

而这个 App 只有菜单栏这一个入口：图标没了，设置、校准、退出、热词全都
点不到了 —— App 明明是好的，用户却打不开它。

- 启动时**显式** `statusItem.isVisible = true`
- 顺手给个 `autosaveName`，位置不再每次乱跑
- `app_status.json` 里加 `menuBarVisible`，`verify.sh` 会核对 ——
  在这之前，"图标不见了"这件事在终端里完全不可见

**通用教训：系统替用户记下来的偏好，可能把你唯一的入口关掉。**
可有可无的东西可以尊重那条记录，唯一入口不行。

## 每次改动后必做

```bash
./install.sh          # 装完会自动跑 verify.sh
./scripts/verify.sh   # 也可以单独跑
```

`verify.sh` 查的正是上面这些坑。**它是自动跑的，因为"记得去查"恰恰是
之前失败的那一环。**

自动更新装好之后，每次更新完也会自动跑一遍，有回归会弹通知。

改完还要跑：

```bash
python3 -m pytest server -q
shellcheck scripts/*.sh install.sh
```

## 热词自动学习的三条边界

做之前用户明确担心过隐私。定下来的边界，改动时不要越过：

1. **只读本机的转写记录**，不读输入框、不碰别的 App、不联网。
   （最初的设计是读当前输入框来学词，候选更准，但要去读用户正在写的
   邮件、密码框旁的字段、别人发来的消息 —— 换掉了。）
2. **只读「原始」行**，不读「输出」。读输出等于把后处理结果喂回去自我强化。
3. **候选词没有任何自动入表的路径。** 词表是偏置解码器用的，听错的词进表
   会让模型错得更稳定 —— 比不加还糟。勾选和「加入」必须是人做的动作。
   同理只挖英文词：中文分不清"常用术语"和"听错的字"。

## 纠正学习（learnCorrections，默认关）

唯一会碰输入框的功能。**证据强度不同，待遇才不同**：用户改过的字是硬证据
（这个词确实一直被听错），而"说得多"不是 —— 所以前者可以自动入表，后者必须人工确认。

不能越过的三条：

- **只按范围读我们自己写的那一段**（`kAXStringForRange`）。承诺要落在接口
  这一层，不能是"读回全文之后我们自觉不看别的"。
- **控件不支持按范围读就放弃这次学习**，绝不退回去读全文。
- **只认小改动**（≤12 字）。整段重写是"我改主意了"，不是听写错误。

门槛 3 次：一次手滑，两次巧合，三次才是规律。

### 11. 口语词过滤默认开着 —— 同一条规矩漏了一个

叠词合并当初因为"删错的代价远大于留噪音"关掉了，`stripFillers` 却一直开着。
同一条规矩，我漏了一个。用户按下说话键说「呃，这个东西…」，那个「呃」
被后处理直接删掉，而我在音频那条路上（门限、回滞、前瞻、启动延迟）
查了好几轮 —— **那个字从来就没丢过。**

- `stripFillers` 默认 false。至此**默认配置下没有任何一步会删字**。
- 有测试钉住：默认配置下，输出的非标点字符必须跟输入逐字相同。
- **下次遇到"某个字没了"，先开 `rawOutput` 说一遍。** 原文里有就是后处理
  删的，原文里没有才是音频或模型的问题 —— 这两件事的解法完全相反。
  这个开关就是为此存在的，我自己却没先用。

## 不要做的事

- 不要把"删内容"类的处理默认打开
- 不要用停顿时长去猜标点
- 不要为了某个增强功能牺牲"能听见/能出字"这条底线
- 不要在没有实测数据时让用户对着滑块猜数值（该给校准工具）
- 不要对用户说"检查一下权限"就完事
